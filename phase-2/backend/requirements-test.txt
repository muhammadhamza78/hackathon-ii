# Test Dependencies (without psycopg2-binary which requires PostgreSQL native libs)
# Using newer versions with pre-built wheels for Python 3.14

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20

# Database & ORM (SQLite only for tests)
sqlmodel>=0.0.22

# Authentication & Security
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1  # Pin bcrypt to version compatible with passlib
python-jose[cryptography]>=3.3.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
email-validator>=2.0.0

# Testing
pytest>=8.3.0
httpx>=0.27.0  # Required by TestClient
