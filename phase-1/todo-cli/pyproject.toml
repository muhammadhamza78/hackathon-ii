[project]
name = "todo-cli"
version = "1.0.0"
description = "Beautiful command-line todo application"
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.0",
]

[project.scripts]
todo = "todo_cli.cli:todo"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"